import { Deps, Props, getWeeklyNumberOfSetsByMuscleIdResolver } from "./get-weekly-number-of-sets-by-muscle-id-resolver";

describe("getWeeklyNumberOfSetsByMuscleIdResolver", () => {
  test.todo("テスト", async () => {
    const deps: Deps = {
    }
    const props: Props = {
    }

    await getWeeklyNumberOfSetsByMuscleIdResolver(deps)(props)
  });
});